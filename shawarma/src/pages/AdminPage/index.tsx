import { AdminPageContainer, Title } from './styles';

const AdminPage = () => {
  return (
    <AdminPageContainer>
      <Title>Admin Only Page</Title>
    </AdminPageContainer>
  );
};

export default AdminPage;
